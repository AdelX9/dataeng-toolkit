services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: demo
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: demo
    ports:
      - "5432:5432"
    volumes:
      - ./examples/sample_data.sql:/docker-entrypoint-initdb.d/01-init.sql
